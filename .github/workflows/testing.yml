name: Run action
on:
  push:
    branches:
      - main
  pull_request:
    branches: [ "main" ]
jobs:
  deploy_live_website:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: apt install opencv-dev 
